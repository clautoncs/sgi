services:
  app:
    build: .
    container_name: sgi
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /root/google-credentials.json:/app/google-credentials.json:ro
    ports:
      - "127.0.0.1:3000:3000"
